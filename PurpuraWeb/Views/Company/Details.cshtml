@model CompanyViewModel

<div class="details-container">
    <h1>@Model.Name</h1>
    <dl>
        <dt>Address</dt>
        <dd>@Model.FullAddress</dd>

        <dt>Details</dt>
        <dd>@Model.Details</dd>
    </dl>
</div>